import { DurationToSPipe } from './duration-to-s.pipe';

describe('DurationToSPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationToSPipe();
    expect(pipe).toBeTruthy();
  });
});
